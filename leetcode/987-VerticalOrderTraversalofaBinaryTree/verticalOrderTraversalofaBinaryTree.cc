/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode(int x) : val(x), left(NULL), right(NULL) {}
 * };
 */
#include "bt.h"
#include <vector>

using std::vector;

// Amazon Timetream team (01/30/2020)
class Solution
{
public:
  vector<vector<int>> verticalTraversal(TreeNode *root)
  {
  }
};

using ptr2verticalTraversal = vector<vector<int>> (Solution::*)(TreeNode*);

void test(ptr2verticalTraversal pfcn) {
  
}

int main()
{
  
}
