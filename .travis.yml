sudo: false
dist: trusty
language: cpp, rust
rust: nightly

compiler:
  - gcc

os:
  - osx

before_script:
- cmake .

script:

- make all
- "for a in $(ls | grep '^[0-9]*$'); do ./$a; done"
- cd leetcode/rust/ && cargo test